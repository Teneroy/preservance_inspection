
const geometry = {
    CONE: "cone",
    SPHERE: "sphere"
};

const sizes = {
    width: window.innerWidth,
    height: window.innerHeight
};

const previewMargin = {
    left: 0,
    top: 0
};

export { geometry, sizes, previewMargin };